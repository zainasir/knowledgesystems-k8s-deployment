apiVersion: v1
kind: Service
metadata:
  labels:
    run: {{ .Values.cbioportalBackendGeniePublicBetaHelm.label }}
  name: {{ .Values.cbioportalBackendGeniePublicBetaHelm.label }}
  selfLink: /api/v1/namespaces/default/services/{{ .Values.cbioportalBackendGeniePublicBetaHelm.label }}
spec:
  ports:
    - port: 80
      name: http
      targetPort: 8080
  selector:
    run: {{ .Values.cbioportalBackendGeniePublicBetaHelm.label }}
  type: ClusterIP