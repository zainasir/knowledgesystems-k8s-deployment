apiVersion: v1
kind: Service
metadata:
  labels:
    run: {{ .Values.backendGeniePublicHelm.name }}
  name: {{ .Values.backendGeniePublicHelm.name }}
  selfLink: {{ .Values.backendGeniePublicHelm.serviceSelfLink }}
spec:
  ports:
    - port: 80
      name: http
      targetPort: 8888
  selector:
    run: {{ .Values.backendGeniePublicHelm.name }}
  type: ClusterIP